# Prompt Contracts

These files define future LLM-assisted gateway behavior. They are not merged output themselves; deterministic scripts and human review remain the source of truth.

A GitHub Action or local Hermes run may use these prompts after a maintainer approves LLM processing for a source package.
