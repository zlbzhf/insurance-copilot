# Schema Proposals

Schema proposals live here when real source processing reveals that the current standard is insufficient.

Use this directory for reviewed changes, not ad hoc preferences. Each proposal should cite triggering source IDs and include migration impact.
