# Synthetic Local Connector Workspace

This fixture is synthetic and public-safe. It demonstrates how `scripts/local_file_connectors.py` reads a private-workspace-shaped directory without real customer or carrier data.

Run:

```bash
python3 scripts/local_file_connectors.py daily-workbench --workspace examples/local-connectors/synthetic-agent-workspace --format markdown
```
