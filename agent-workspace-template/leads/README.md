# Leads

Private lead records belong here in a real agent workspace. The public template uses only blank/synthetic files.

Use one file per lead, based on `template-lead.md`. Do not publish private copies.
